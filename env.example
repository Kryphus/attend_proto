# Example .env file for attend_proto
# Copy this to .env and modify values as needed

# Environment: dev or prod
ENVIRONMENT=dev

# Heartbeat interval in minutes (how often to check location/presence)
# Dev default: 1, Prod default: 60
HEARTBEAT_INTERVAL_MINUTES=1

# Sync interval in minutes (how often to sync with server)
# Dev default: 1, Prod default: 60
SYNC_INTERVAL_MINUTES=1

# Biometric freshness in minutes (how long biometric auth is valid)
# Default: 5
BIOMETRIC_FRESHNESS_MINUTES=5

# Geofence radius in meters
# Default: 100
GEOFENCE_RADIUS_METERS=100

# Maximum retry backoff in minutes
# Dev default: 10, Prod default: 120
MAX_RETRY_BACKOFF_MINUTES=10
